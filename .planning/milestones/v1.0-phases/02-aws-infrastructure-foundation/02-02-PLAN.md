---
phase: 02-aws-infrastructure-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [lib/task-definition.ts, lib/task-definition.js, lib/redis.ts, lib/redis.js]
autonomous: true
requirements: [INFRA-01, INFRA-03]

must_haves:
  truths:
    - Fargate tasks use 0.25 vCPU and 0.5GB RAM (cost-optimized sizing)
    - Redis cluster uses cache.t4g.micro instance type (Graviton2)
    - Task definition environment variables pass Redis endpoint/port to container
  artifacts:
    - lib/task-definition.ts with cpu: 256, memoryLimitMiB: 512
    - lib/redis.ts with cacheNodeType: 'cache.t4g.micro'
    - Compiled JavaScript files for both modules
  key_links:
    - Task definition receives redisEndpoint and redisPort as props
    - Container environment variables REDIS_ENDPOINT and REDIS_PORT set from props
    - Redis Multi-AZ configuration preserved (automaticFailoverEnabled: true)
---

<objective>
Right-size Fargate task resources and update Redis instance type for cost optimization

Purpose: Reduce ECS task cost to ~$6/mo per task (from current larger allocation) and use Graviton2 Redis for better price/performance
Output: Updated task definition with 0.25 vCPU / 0.5GB RAM, Redis using t4g.micro instance type
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-aws-infrastructure-foundation/02-RESEARCH.md
@.planning/phases/02-aws-infrastructure-foundation/02-CONTEXT.md
@lib/task-definition.js
@lib/redis.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update task definition resource allocation</name>
  <files>lib/task-definition.ts, lib/task-definition.js</files>
  <action>
Create TypeScript source `lib/task-definition.ts` and rebuild to update resource allocation.

**User decision (locked):** CPU: 0.25 vCPU (256 units), Memory: 0.5GB (512 MiB)

Changes:
1. FargateTaskDefinition constructor: `cpu: 256, memoryLimitMiB: 512`
2. Container addContainer call: `cpu: 256, memoryLimitMiB: 512` (same values - single container)
3. Add CloudWatch logging with 7-day retention (cost optimization per Claude's discretion):
   ```typescript
   import { LogDriver } from 'aws-cdk-lib/aws-ecs';
   import { RetentionDays } from 'aws-cdk-lib/aws-logs';

   logging: LogDriver.awsLogs({
     streamPrefix: 'websocket-gateway',
     logRetention: RetentionDays.ONE_WEEK,
   })
   ```

**Why these values:** Fargate pricing is $0.04048/vCPU-hour + $0.004445/GB-hour. At 0.25 vCPU + 0.5GB = ~$6/mo per task. User specified this sizing for <1000 connection workload (500 connections/task capacity).

**Preserve:** Environment variable handling for REDIS_ENDPOINT and REDIS_PORT from props.

After creating TypeScript source, compile: `npm run build` or `npx tsc`
  </action>
  <verify>
- lib/task-definition.ts exists with cpu: 256 and memoryLimitMiB: 512 in both task and container
- Logging configuration added with ONE_WEEK retention
- Compiled JavaScript file updated (lib/task-definition.js modified timestamp newer than compile time)
- CDK synth succeeds: `npx cdk synth --quiet`
- Synth output shows TaskDefinition with Cpu: 256 and Memory: 512
  </verify>
  <done>
Task definition configured for 0.25 vCPU and 0.5GB RAM with CloudWatch logging, compiled to JavaScript
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update Redis instance type to Graviton2</name>
  <files>lib/redis.ts, lib/redis.js</files>
  <action>
Create TypeScript source `lib/redis.ts` and rebuild to update Redis instance type.

**User decision (locked):** Instance type: cache.t4g.micro (Graviton2, 0.5GB memory)

Change in CfnReplicationGroup:
- `cacheNodeType: 'cache.t4g.micro'` (currently 'cache.t3.micro' in compiled code)

**Preserve ALL other Redis configuration:**
- `numNodeGroups: 1` (cluster mode disabled - correct for pub/sub)
- `replicasPerNodeGroup: 1` (primary + 1 replica = Multi-AZ)
- `automaticFailoverEnabled: true` (Multi-AZ requirement)
- Security group configuration
- Subnet group configuration
- Return object with endpoint, port, securityGroup

**Why t4g.micro:** Same price as t3.micro (~$12/mo Multi-AZ), better performance (Graviton2 ARM), research confirmed standard choice.

After creating TypeScript source, compile: `npm run build` or `npx tsc`
  </action>
  <verify>
- lib/redis.ts exists with cacheNodeType: 'cache.t4g.micro'
- automaticFailoverEnabled: true preserved (Multi-AZ requirement)
- replicasPerNodeGroup: 1 preserved (primary + replica)
- Compiled JavaScript file updated (lib/redis.js modified timestamp newer)
- CDK synth succeeds: `npx cdk synth --quiet`
- Synth output shows CfnReplicationGroup with CacheNodeType: cache.t4g.micro
  </verify>
  <done>
Redis configured for cache.t4g.micro with Multi-AZ failover preserved, compiled to JavaScript
  </done>
</task>

</tasks>

<verification>
After tasks complete:
1. Verify task resources: `npx cdk synth | grep -A 3 "TaskDefinition"` shows Cpu: 256, Memory: 512
2. Verify Redis instance: `npx cdk synth | grep "CacheNodeType"` shows cache.t4g.micro
3. Verify Multi-AZ preserved: `npx cdk synth | grep "AutomaticFailoverEnabled"` shows true
4. Verify CloudWatch log retention: `npx cdk synth | grep -A 2 "RetentionInDays"` shows 7
</verification>

<success_criteria>
- Task definition uses 256 CPU units (0.25 vCPU) and 512 MiB RAM (0.5GB)
- Redis uses cache.t4g.micro instance type (Graviton2)
- Multi-AZ automatic failover configuration preserved
- CloudWatch log retention set to 7 days
- CDK synth produces valid CloudFormation template
- Estimated monthly cost: ~$6/mo per ECS task, ~$12/mo for Redis Multi-AZ
</success_criteria>

<output>
After completion, create `.planning/phases/02-aws-infrastructure-foundation/02-02-SUMMARY.md`
</output>
