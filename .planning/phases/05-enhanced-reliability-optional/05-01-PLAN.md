---
phase: 05-enhanced-reliability-optional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/cursor-service.js
  - src/services/chat-service.js
  - src/services/presence-service.js
  - src/core/message-router.js
  - test/redis-degradation.test.js
autonomous: true
requirements:
  - REL-04

must_haves:
  truths:
    - "Cursor updates succeed when Redis is unavailable"
    - "Chat messages delivered to local clients when Redis down"
    - "Presence updates work in local-only mode during Redis outage"
    - "Services automatically restore Redis operations when connection recovers"
  artifacts:
    - path: "src/services/cursor-service.js"
      provides: "Redis connection event handlers with local cache fallback"
      contains: "redisAvailable"
    - path: "src/services/chat-service.js"
      provides: "Redis degradation handling for chat broadcasts"
      contains: "redisAvailable"
    - path: "src/services/presence-service.js"
      provides: "Redis degradation handling for presence updates"
      contains: "redisAvailable"
    - path: "test/redis-degradation.test.js"
      provides: "Automated tests for Redis fallback behavior"
      min_lines: 100
  key_links:
    - from: "src/services/cursor-service.js"
      to: "this.messageRouter.redisPublisher"
      via: "error event listener"
      pattern: "redisPublisher.*on\\('error'"
    - from: "src/services/cursor-service.js"
      to: "this.clientCursors Map"
      via: "local cache fallback during outage"
      pattern: "redisAvailable.*false.*clientCursors"
---

<objective>
Implement graceful Redis degradation so services continue operating with local cache when Redis becomes unavailable, then restore distributed operations when Redis reconnects.

Purpose: Ensure high availability - Redis outages should not cause connection drops or service interruptions. Services degrade to local-only mode, maintaining functionality for clients connected to the same node.

Output: Redis-resilient services with automatic failover to local cache and recovery, validated by automated tests simulating Redis outages.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-enhanced-reliability-optional/05-RESEARCH.md

<interfaces>
<!-- Existing service architecture from codebase -->

From src/services/cursor-service.js:
```javascript
class CursorService {
  constructor(messageRouter, logger, metricsCollector = null) {
    this.messageRouter = messageRouter;
    this.clientCursors = new Map(); // Local cache already exists
    this.channelCursors = new Map();
    this.isDistributed = !!messageRouter;
  }
}
```

From src/core/message-router.js:
```javascript
class MessageRouter {
  constructor(nodeManager, redisPublisher, redisSubscriber, logger) {
    this.redisPublisher = redisPublisher; // Need to monitor this connection
    this.redisSubscriber = redisSubscriber; // Need to monitor this connection
    this.localClients = new Map();
  }

  async sendToChannel(channel, message) {
    // Currently publishes to Redis - need fallback
  }
}
```

From 05-RESEARCH.md Pattern 1:
```javascript
// Redis client error event handling
redis.on('error', (err) => {
  if (['ECONNREFUSED','ECONNRESET','ETIMEDOUT'].includes(err.code)) {
    this.redisAvailable = false;
  }
});

redis.on('ready', () => {
  this.redisAvailable = true;
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Redis connection health monitoring</name>
  <files>src/core/message-router.js, test/redis-degradation.test.js</files>
  <behavior>
    - Test 1: redisAvailable set to false on ECONNREFUSED error from redisPublisher
    - Test 2: redisAvailable set to true on 'ready' event from redisPublisher
    - Test 3: sendToChannel falls back to local-only broadcast when redisAvailable=false
    - Test 4: sendToChannel resumes Redis publishing when redisAvailable=true
  </behavior>
  <action>
Add Redis connection health tracking to MessageRouter:

1. Add redisAvailable state flag (default true)
2. Register error event listeners on redisPublisher and redisSubscriber:
   - On error with code ECONNREFUSED, ECONNRESET, ETIMEDOUT, or EAI_AGAIN: set redisAvailable = false, log warning "Redis unavailable, using local cache"
   - On ready event: set redisAvailable = true, log info "Redis connection restored"
3. Modify sendToChannel() to check redisAvailable:
   - If true: publish to Redis as normal (existing behavior)
   - If false: only call broadcastToLocalChannel() (skip Redis publish, log at debug level)
4. Modify subscribeToRedisChannel() to track subscription attempts but not fail if Redis unavailable

Use pattern from 05-RESEARCH.md Pattern 1 (lines 76-98). Handle Redis connection lifecycle without breaking existing pub/sub functionality.

Create test/redis-degradation.test.js:
- Mock RedisPublisher/RedisSubscriber with event emitters
- Verify MessageRouter.redisAvailable toggles correctly on events
- Verify sendToChannel behavior changes based on redisAvailable flag
- Verify local clients still receive messages when Redis down</action>
  <verify>
    <automated>npm test -- test/redis-degradation.test.js -x</automated>
  </verify>
  <done>MessageRouter tracks Redis health via connection events, falls back to local-only broadcasting during outages, and automatically restores Redis publishing on reconnection. Tests confirm behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enhance services with Redis fallback logic</name>
  <files>src/services/cursor-service.js, src/services/chat-service.js, src/services/presence-service.js, test/cursor-service-fallback.test.js, test/chat-service-fallback.test.js</files>
  <behavior>
    - Test 1: CursorService writes to local clientCursors Map when messageRouter.redisAvailable=false
    - Test 2: ChatService delivers messages to local clients only when Redis unavailable
    - Test 3: PresenceService updates local state without Redis during outage
    - Test 4: All services resume Redis operations when messageRouter.redisAvailable becomes true
  </behavior>
  <action>
Update cursor-service.js:
1. Check messageRouter.redisAvailable before Redis operations in handleUpdateCursor()
2. If false: only update this.clientCursors and this.channelCursors Maps (skip Redis writes)
3. If true: perform existing cache-aside pattern (write local, then Redis)
4. Log at debug level: "Redis unavailable, cursor cached locally only" when degraded

Update chat-service.js:
1. Check messageRouter.redisAvailable in handleSendMessage()
2. If false: broadcast via messageRouter.broadcastToLocalChannel() only (no cross-node delivery)
3. Add log: "Redis unavailable, message delivered to local clients only"
4. Keep existing channelHistory LRU cache working (already local)

Update presence-service.js:
1. Check messageRouter.redisAvailable in handleSetPresence()
2. If false: update local clientPresence Map only, broadcast via local channel
3. If true: existing behavior (write local, broadcast via Redis pub/sub)
4. Add log: "Redis unavailable, presence update local only"

Create test/cursor-service-fallback.test.js and test/chat-service-fallback.test.js:
- Mock messageRouter with redisAvailable=false
- Verify cursor updates succeed and write to local Maps
- Verify chat messages reach local clients
- Verify no errors thrown when Redis unavailable</action>
  <verify>
    <automated>npm test -- --testPathPattern="(cursor|chat)-service-fallback" -x</automated>
  </verify>
  <done>All services gracefully degrade to local cache when Redis unavailable. Cursor updates, chat messages, and presence status continue functioning for clients on the same node. No connection drops or errors during Redis outages. Tests confirm fallback behavior.</done>
</task>

</tasks>

<verification>
**Automated:**
- Run full test suite: `npm test`
- Verify no regression in existing Redis-dependent tests
- Confirm new degradation tests pass

**Manual simulation:**
1. Start server with Redis running
2. Send cursor updates and chat messages (should work normally)
3. Stop Redis container/service
4. Send cursor updates and chat messages (should still work for local clients, logs show degraded mode)
5. Restart Redis
6. Send messages (should resume cross-node delivery, logs show "Redis connection restored")
</verification>

<success_criteria>
- [ ] MessageRouter tracks redisAvailable state via error/ready events
- [ ] sendToChannel() falls back to local-only broadcast when Redis down
- [ ] CursorService writes to local Maps during Redis outage
- [ ] ChatService delivers to local clients during Redis outage
- [ ] PresenceService updates local state during Redis outage
- [ ] All services automatically resume Redis operations on reconnection
- [ ] test/redis-degradation.test.js passes with 100% coverage of fallback logic
- [ ] No connection drops occur when Redis becomes unavailable
- [ ] Logs clearly indicate degraded mode and recovery
</success_criteria>

<output>
After completion, create `.planning/phases/05-enhanced-reliability-optional/05-01-SUMMARY.md`
</output>
