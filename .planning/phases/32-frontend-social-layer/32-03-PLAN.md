---
phase: 32-frontend-social-layer
plan: 03
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
files_modified:
  - frontend/src/components/AppLayout.tsx
  - frontend/src/app/App.tsx
  - frontend/.env
  - frontend/.env.example
autonomous: false
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - SOCL-01
  - SOCL-02
  - SOCL-03
  - SOCL-04
  - SOCL-05
  - SOCL-06
  - GRUP-01
  - GRUP-02
  - GRUP-03
  - GRUP-04
  - GRUP-05
  - GRUP-06
  - GRUP-07
  - GRUP-08
  - GRUP-09
  - ROOM-01
  - ROOM-02
  - ROOM-03
  - ROOM-04
  - ROOM-05
  - ROOM-06
  - ROOM-07
  - ROOM-08
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - CONT-08
  - REAC-01
  - REAC-02
  - REAC-03
  - REAC-04
  - REAC-05
  - REAC-06
  - RTIM-01
  - RTIM-02
  - RTIM-03
  - RTIM-04

must_haves:
  truths:
    - "AppLayout renders GroupPanel, RoomList, PostFeed in the main content area (below existing sections)"
    - "SocialPanel in AppLayout receives idToken and onMessage props — not zero-argument"
    - "App.tsx passes auth.idToken and the onMessage registrar to AppLayout's new social props"
    - "frontend app compiles and loads in the browser without errors"
    - "Social section shows GroupPanel, RoomList, PostFeed alongside the existing SocialPanel"
  artifacts:
    - path: "frontend/src/components/AppLayout.tsx"
      provides: "AppLayout with social section wired"
      contains: "GroupPanel"
    - path: "frontend/src/app/App.tsx"
      provides: "GatewayDemo with social props passed"
      contains: "idToken"
  key_links:
    - from: "frontend/src/app/App.tsx"
      to: "frontend/src/components/AppLayout.tsx"
      via: "auth.idToken and onMessage passed as new AppLayoutProps"
      pattern: "idToken.*auth.idToken"
    - from: "frontend/src/components/AppLayout.tsx"
      to: "frontend/src/components/GroupPanel.tsx"
      via: "import GroupPanel and render in main content"
      pattern: "GroupPanel"
    - from: "frontend/src/components/AppLayout.tsx"
      to: "frontend/src/components/PostFeed.tsx"
      via: "import PostFeed, pass activeRoomId from RoomList selection"
      pattern: "PostFeed"

user_setup:
  - service: social-api
    why: "Social API must be running locally for hooks to fetch live data"
    env_vars:
      - name: VITE_SOCIAL_API_URL
        source: "frontend/.env — set to http://localhost:3001 for local development"
    dashboard_config: []
---

<objective>
Wire the Phase 32 social components into the running app. AppLayout.tsx receives new props (idToken, onMessage, activeRoomId) and renders GroupPanel, RoomList, and PostFeed in the social section. App.tsx (GatewayDemo) passes auth.idToken and the existing onMessage registrar through. A checkpoint verifies the full social UI is visible and functional in the browser.

Purpose: Final integration step — social features become accessible to the authenticated user.
Output: Updated AppLayout.tsx and App.tsx. Checkpoint confirms visual verification.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-frontend-social-layer/32-UI-SPEC.md
@.planning/phases/32-frontend-social-layer/32-02-SUMMARY.md

<interfaces>
<!-- AppLayout current props (from AppLayout.tsx) -->
AppLayoutProps currently has:
  connectionState, currentChannel, onSwitchChannel, onDisconnect, onReconnect,
  userEmail, onSignOut, presenceUsers, currentClientId, activeReactions, onReact,
  chatMessages, onChatSend, onTyping, cursors, activeMode, onModeChange,
  onFreeformMove, onTableClick, onTextChange, onCanvasMove,
  crdtContent, applyLocalEdit, logEntries, errors, lastError, clientId, sessionToken

Currently renders: <SocialPanel /> with no props (zero-argument call — must be updated).

<!-- New props to add to AppLayoutProps -->
  idToken: string | null              // auth.idToken from GatewayDemo
  onMessage: OnMessageFn              // the onMessage registrar from GatewayDemo

<!-- Component signatures (from plans 32-01, 32-02) -->
SocialPanel: ({ idToken, onMessage })
GroupPanel:  ({ idToken })
RoomList:    ({ idToken, onRoomSelect, activeRoomId? })
PostFeed:    ({ idToken, roomId, onMessage })

<!-- GatewayDemo in App.tsx already has: -->
  const onMessage = (handler) => { ... }   // stable registrar
  auth.idToken                             // from UseAuthReturn
  All hook calls already use these — adding to AppLayout is additive only

<!-- OnMessageFn type (matches existing pattern in hooks) -->
type OnMessageFn = (handler: (msg: GatewayMessage) => void) => () => void;
Import GatewayMessage from '../types/gateway' in AppLayout.tsx.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add social props to AppLayout and render GroupPanel, RoomList, PostFeed</name>
  <files>
    frontend/src/components/AppLayout.tsx
    frontend/src/app/App.tsx
  </files>
  <read_first>
    - frontend/src/components/AppLayout.tsx (full file — understand current AppLayoutProps and JSX structure)
    - frontend/src/app/App.tsx (full file — understand GatewayDemo and how to pass new props)
    - frontend/src/components/GroupPanel.tsx (just created — props interface)
    - frontend/src/components/RoomList.tsx (just created — props interface)
    - frontend/src/components/PostFeed.tsx (just created — props interface)
  </read_first>
  <action>
**AppLayout.tsx — additive changes only**

1. Add imports at top:
```tsx
import { GroupPanel } from './GroupPanel';
import { RoomList } from './RoomList';
import { PostFeed } from './PostFeed';
import type { GatewayMessage } from '../types/gateway';
```

2. Define OnMessageFn type at module level (before AppLayoutProps):
```tsx
type OnMessageFn = (handler: (msg: GatewayMessage) => void) => () => void;
```

3. Add to AppLayoutProps interface:
```tsx
idToken: string | null;
onMessage: OnMessageFn;
```

4. Add to the destructured props in the AppLayout function signature:
```tsx
idToken,
onMessage,
```

5. Add `activeRoomId` local state inside AppLayout:
```tsx
const [activeRoomId, setActiveRoomId] = useState<string | null>(null);
```
Import `useState` (already imported).

6. Update the existing `<SocialPanel />` call to pass props:
```tsx
<SocialPanel idToken={idToken} onMessage={onMessage} />
```

7. After the existing SocialPanel section card, add three new section cards:
```tsx
{/* Groups section */}
<GroupPanel idToken={idToken} />

{/* Rooms section */}
<RoomList
  idToken={idToken}
  onRoomSelect={(room) => setActiveRoomId(room.roomId)}
  activeRoomId={activeRoomId}
/>

{/* Posts section */}
<PostFeed idToken={idToken} roomId={activeRoomId} onMessage={onMessage} />
```

---

**App.tsx — additive changes only**

In the `<AppLayout ... />` JSX call inside GatewayDemo, add two new props:
```tsx
idToken={auth.idToken}
onMessage={onMessage}
```

The `onMessage` variable is already defined in GatewayDemo (the registrar closure). `auth.idToken` is already available as `auth` is a GatewayDemo prop. No other changes.

---

**frontend/.env** — if file exists, append `VITE_SOCIAL_API_URL=http://localhost:3001` on a new line if not already present. If .env does not exist (executor should check with `ls frontend/.env`), do not create it — only .env.example is guaranteed.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/websocker_gateway/frontend && npx tsc --noEmit 2>&1 | head -30; echo "TypeScript exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "GroupPanel\|RoomList\|PostFeed" frontend/src/components/AppLayout.tsx` returns at least 6 matches (3 imports + 3 JSX uses)
    - `grep -n "idToken.*onMessage\|idToken: string" frontend/src/components/AppLayout.tsx` returns a match (new props)
    - `grep -n "idToken={auth.idToken}" frontend/src/app/App.tsx` returns a match
    - `grep -n "onMessage={onMessage}" frontend/src/app/App.tsx` returns a match
    - `grep -n "SocialPanel idToken=" frontend/src/components/AppLayout.tsx` returns a match (updated SocialPanel call)
    - `npx tsc --noEmit` in frontend/ exits with code 0 (zero TypeScript errors)
  </acceptance_criteria>
  <done>AppLayout renders GroupPanel, RoomList, PostFeed. App.tsx passes idToken and onMessage. Full project compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — social UI functional in browser</name>
  <what-built>
    The complete Phase 32 social UI — 7 hooks + 5 components wired into AppLayout. The authenticated app now shows:
    - Social Profile section (live profile data + friends graph via useSocialProfile/useFriends)
    - Groups section (GroupPanel with create, join, leave, member list)
    - Rooms section (RoomList with create standalone, DM room, room selection)
    - Posts section (PostFeed for the selected room with like, react, inline comments)
  </what-built>
  <how-to-verify>
    Prerequisites: social-api must be running (`cd social-api && npm run dev`), frontend must be running (`cd frontend && npm run dev`), VITE_SOCIAL_API_URL=http://localhost:3001 must be in frontend/.env.

    1. Open http://localhost:5173 and sign in with a valid Cognito test user.
    2. Scroll to the "Social Profile" section — confirm it shows a profile card (not mock data, not MockDataBanner).
       - If profile doesn't exist yet, the card should show "Loading profile…" then an empty state (no crash).
    3. Scroll to "Groups" section — confirm GroupPanel renders with "Create Group" button and empty state "No groups yet".
    4. Click "Create Group" — confirm the inline form appears with name input, description, visibility radio.
    5. Create a group — confirm it appears in the GroupList.
    6. Scroll to "Rooms" section — confirm RoomList renders with "Create Room" button.
    7. Create a room — confirm it appears in the room list with a "standalone" type badge.
    8. Click the room — confirm it becomes highlighted with a left blue accent border.
    9. Scroll to "Posts" section — confirm PostFeed renders for the selected room with "No posts yet" empty state.
    10. Type a post and click "Post" — confirm it appears in the feed.
    11. Click "Comment" on the post — confirm CommentThread expands inline below the post.
    12. Click "❤" (like button) — confirm the count increments and the heart turns accent (#646cff).
    13. Open a second browser tab with a different test user, join the same room — confirm posts from tab 1 appear in tab 2 via WebSocket (social:post event).
    14. Open browser DevTools Console — confirm zero uncaught JavaScript errors.
  </how-to-verify>
  <action>Verify the social UI is fully functional in the browser by following the steps in how-to-verify. This is a human verification step — no code changes.</action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/websocker_gateway/frontend && npx tsc --noEmit 2>&amp;1 | head -5; echo "TypeScript exit: $?"</automated>
  </verify>
  <done>All 14 verification steps pass and browser console shows zero uncaught errors.</done>
  <resume-signal>Type "approved" when all checks pass, or describe any failing step.</resume-signal>
</task>

</tasks>

<verification>
TypeScript compile check (automated):
```bash
cd /Users/connorhoehn/Projects/websocker_gateway/frontend && npx tsc --noEmit
```
Expected: exit code 0, zero errors.

Import wiring check:
```bash
grep -n "GroupPanel\|RoomList\|PostFeed" frontend/src/components/AppLayout.tsx
```
Expected: 3 import lines + 3 JSX usage lines.

Props passed from GatewayDemo:
```bash
grep -n "idToken={auth.idToken}\|onMessage={onMessage}" frontend/src/app/App.tsx
```
Expected: 2 matches.

Social section structure check:
```bash
grep -n "SocialPanel\|GroupPanel\|RoomList\|PostFeed" frontend/src/components/AppLayout.tsx
```
Expected: all four component names present.
</verification>

<success_criteria>
- AppLayout.tsx compiles cleanly with GroupPanel, RoomList, PostFeed imported and rendered
- SocialPanel call in AppLayout.tsx passes idToken and onMessage (no longer zero-argument)
- App.tsx passes auth.idToken and onMessage to AppLayout
- Human checkpoint confirms social features visible and interactive in the browser
- Zero uncaught browser console errors after sign-in
</success_criteria>

<output>
After completion, create `.planning/phases/32-frontend-social-layer/32-03-SUMMARY.md`
</output>
