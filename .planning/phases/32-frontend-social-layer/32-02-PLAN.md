---
phase: 32-frontend-social-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - frontend/src/components/SocialPanel.tsx
  - frontend/src/components/GroupPanel.tsx
  - frontend/src/components/RoomList.tsx
  - frontend/src/components/PostFeed.tsx
  - frontend/src/components/CommentThread.tsx
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - SOCL-01
  - SOCL-02
  - SOCL-03
  - SOCL-04
  - SOCL-05
  - SOCL-06
  - GRUP-01
  - GRUP-02
  - GRUP-03
  - GRUP-04
  - GRUP-05
  - GRUP-06
  - GRUP-07
  - GRUP-08
  - GRUP-09
  - ROOM-01
  - ROOM-02
  - ROOM-03
  - ROOM-04
  - ROOM-05
  - ROOM-06
  - ROOM-07
  - ROOM-08
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - CONT-07
  - CONT-08
  - REAC-01
  - REAC-02
  - REAC-03
  - REAC-04
  - REAC-05
  - REAC-06
  - RTIM-01
  - RTIM-02
  - RTIM-03
  - RTIM-04

must_haves:
  truths:
    - "SocialPanel accepts idToken and onMessage props and calls useSocialProfile/useFriends with live data — MockDataBanner is removed"
    - "GroupPanel renders a create-group form, a scrollable group list, and member management per UI-SPEC layout contract"
    - "GroupPanel's GroupCard shows a Delete button visible only to the group owner — clicking calls deleteGroup and removes the card (GRUP-02)"
    - "RoomList accepts idToken, onMessage, onRoomSelect, and activeRoomId props; passes onMessage to useRooms so social:member_joined and social:member_left events reach the hook (RTIM-04)"
    - "PostFeed renders a CreatePostForm, PostCard list with LikeButton and EmojiReactionBar, and expands CommentThread inline"
    - "CommentThread renders flat CommentItem list, nested replies indented 32px, and ReplyForm"
    - "All components compile with zero TypeScript errors and use inline styles only (no CSS files, no utility classes)"
  artifacts:
    - path: "frontend/src/components/SocialPanel.tsx"
      provides: "SocialPanel with live hooks"
      contains: "useSocialProfile"
    - path: "frontend/src/components/GroupPanel.tsx"
      provides: "GroupPanel exported component"
      exports: ["GroupPanel"]
    - path: "frontend/src/components/RoomList.tsx"
      provides: "RoomList exported component"
      exports: ["RoomList"]
    - path: "frontend/src/components/PostFeed.tsx"
      provides: "PostFeed exported component"
      exports: ["PostFeed"]
    - path: "frontend/src/components/CommentThread.tsx"
      provides: "CommentThread exported component"
      exports: ["CommentThread"]
  key_links:
    - from: "frontend/src/components/SocialPanel.tsx"
      to: "frontend/src/hooks/useSocialProfile.ts"
      via: "import useSocialProfile"
      pattern: "useSocialProfile"
    - from: "frontend/src/components/GroupPanel.tsx"
      to: "frontend/src/hooks/useGroups.ts"
      via: "import useGroups, call useGroups({ idToken }), destructure deleteGroup for GroupCard owner button"
      pattern: "useGroups"
    - from: "frontend/src/components/RoomList.tsx"
      to: "frontend/src/hooks/useRooms.ts"
      via: "import useRooms, call useRooms({ idToken, onMessage }) forwarding onMessage prop"
      pattern: "useRooms"
    - from: "frontend/src/components/PostFeed.tsx"
      to: "frontend/src/hooks/useLikes.ts"
      via: "import useLikes"
      pattern: "useLikes"
    - from: "frontend/src/components/PostFeed.tsx"
      to: "frontend/src/components/CommentThread.tsx"
      via: "expanded CommentThread inline below PostCard"
      pattern: "CommentThread"
---

<objective>
Build the five social UI components. SocialPanel is refactored from mock to live API hooks. GroupPanel, RoomList, PostFeed, and CommentThread are created new. All components follow the UI-SPEC layout contract, use inline styles only, and accept idToken + onMessage as props so App.tsx can wire them in plan 32-03. GroupPanel's GroupCard shows a Delete button for group owners (GRUP-02). RoomList forwards onMessage to useRooms so member join/leave events are handled in real time (RTIM-04).

Purpose: Delivers all visual social features. Plan 32-03 mounts them in AppLayout.
Output: 5 component files, all co-locating sub-components as unexported internals.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-frontend-social-layer/32-UI-SPEC.md
@.planning/phases/32-frontend-social-layer/32-01-SUMMARY.md

<interfaces>
<!-- Hook return shapes (from plan 32-01) -->

useSocialProfile({ idToken }) →
  { profile: ProfileItem|null, loading, error, updateProfile(updates), viewProfile(userId) }

useFriends({ idToken }) →
  { followers: PublicProfile[], following: PublicProfile[], friends: PublicProfile[],
    follow(userId), unfollow(userId), loading, error }

useGroups({ idToken }) →
  { groups: GroupItem[], createGroup(name,desc?,vis?), deleteGroup(groupId),
    joinGroup(groupId), leaveGroup(groupId),
    inviteUser(groupId,userId), acceptInvite(groupId,accept), loadMembers(groupId),
    members: MemberItem[], loading, error }

useRooms({ idToken, onMessage }) →
  { rooms: RoomItem[], activeRoom: RoomItem|null, setActiveRoom(room|null),
    createRoom(name), createDM(peerId), joinRoom(roomId), leaveRoom(roomId),
    members: RoomMemberItem[], loading, error }

usePosts({ idToken, roomId, onMessage }) →
  { posts: PostItem[], createPost(content), editPost(postId,content), deletePost(postId),
    loading, error, hasMore, loadMore }

useComments({ idToken, roomId, postId, onMessage }) →
  { comments: CommentItem[], createComment(content, parentCommentId?), deleteComment(commentId),
    loading, error }

useLikes({ idToken, roomId, postId, commentId?, onMessage }) →
  { isLiked, likeCount, whoLiked: PublicProfile[], toggle(), reactWithEmoji(emoji), loading, error }

<!-- Type shapes needed by components -->
ProfileItem:      { userId, displayName, bio, avatarUrl, visibility: 'public'|'private', createdAt, updatedAt }
PublicProfile:    { userId, displayName, avatarUrl, visibility }
GroupItem:        { groupId, name, description, visibility, ownerId, createdAt }
MemberItem:       { groupId, userId, role: 'owner'|'admin'|'member', joinedAt, displayName? }
RoomItem:         { roomId, channelId, name, type: 'standalone'|'group'|'dm', ownerId, groupId?, dmPeerUserId?, createdAt }
RoomMemberItem:   { roomId, userId, displayName?, joinedAt }
PostItem:         { roomId, postId, authorId, content, createdAt, updatedAt, _fadeIn?: boolean }
CommentItem:      { postId, commentId, authorId, content, parentCommentId?: string, createdAt, _fadeIn?: boolean }

<!-- UI-SPEC: Design System -->
Inline styles only. No CSS files, no utility classes.
Font: system-ui, -apple-system, sans-serif
sectionCardStyle: { background: '#ffffff', border: '1px solid #e2e8f0', borderRadius: 8, padding: '1.25rem' }
sectionHeaderStyle: { fontSize: '0.75rem', fontWeight: 600, textTransform: 'uppercase', letterSpacing: '0.05em', color: '#64748b', margin: '0 0 0.75rem 0' }
Accent: #646cff  Destructive: #dc2626  Muted: #64748b  Body: #374151  Strong: #0f172a
Border/divider: #e2e8f0  Hover bg: #f8fafc  Row hover: #f1f5f9
EMOJIS = ['❤️','😂','👍','👎','😮','😢','😡','🎉','🔥','⚡','💯','🚀']

<!-- Existing SocialPanel.tsx exports and patterns — plan 32-01 summary will have full detail -->
SocialPanel currently uses: MockDataBanner, Avatar, FollowButton, ProfileCard, SocialGraphPanel
All sub-components are unexported internals co-located in the same file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor SocialPanel to live hooks + create GroupPanel and RoomList</name>
  <files>
    frontend/src/components/SocialPanel.tsx
    frontend/src/components/GroupPanel.tsx
    frontend/src/components/RoomList.tsx
  </files>
  <read_first>
    - frontend/src/components/SocialPanel.tsx (full file — existing mock implementation to refactor)
    - frontend/src/hooks/useSocialProfile.ts (just created — exact return shape)
    - frontend/src/hooks/useFriends.ts (just created)
    - frontend/src/hooks/useGroups.ts (just created — includes deleteGroup for GRUP-02)
    - frontend/src/hooks/useRooms.ts (just created — accepts onMessage for RTIM-04)
    - .planning/phases/32-frontend-social-layer/32-UI-SPEC.md (layout contracts for all three)
  </read_first>
  <action>
**SocialPanel.tsx — refactor existing file**

Remove: all mock data constants (CURRENT_USER, MOCK_USERS, TAB_FOLLOWERS, TAB_FOLLOWING, TAB_FRIENDS), the MockDataBanner component.

Change `SocialPanel` signature to:
```tsx
export function SocialPanel({ idToken, onMessage }: { idToken: string | null; onMessage: OnMessageFn })
```
where `type OnMessageFn = (handler: (msg: import('../types/gateway').GatewayMessage) => void) => () => void`.

Inside SocialPanel, call:
```tsx
const { profile, loading: profileLoading, updateProfile } = useSocialProfile({ idToken });
const { followers, following, friends, follow, unfollow } = useFriends({ idToken });
```

Pass real data to ProfileCard (replace `user={currentUser}`) and SocialGraphPanel. `isOwnProfile={true}` stays. `onSave` calls `updateProfile`. SocialGraphPanel receives `followers`, `following`, `friends`, real follow states derived from `following` array (user is 'following' if their userId appears in the `following` array). `onFollowChange` calls `follow(userId)` or `unfollow(userId)` then re-derives follow states.

Keep Avatar, FollowButton, ProfileCard, SocialGraphPanel as unexported internals (no structural changes to those sub-components). Update ProfileCard's `onSave` handler to call `updateProfile` (async — set saving=true, await, set saving=false). Add loading skeleton: if `profileLoading && !profile`, render a `<div style={{padding: 40, textAlign: 'center', color: '#9ca3af', fontSize: 14}}>Loading profile…</div>`.

---

**GroupPanel.tsx — new file**

Structure per UI-SPEC GroupPanel layout contract. Props: `{ idToken: string | null }`.

```tsx
export function GroupPanel({ idToken }: { idToken: string | null })
```

Inside: call `useGroups({ idToken })`. Destructure `groups, createGroup, deleteGroup, joinGroup, leaveGroup, inviteUser, loadMembers, members, loading`.

Identify current user: `const currentUserId = idToken ? (JSON.parse(atob(idToken.split('.')[1])) as { sub: string }).sub : null`.

Sub-components (all unexported internals):
- `CreateGroupForm`: inline form that appears when "Create Group" is clicked. Fields: name (input, required, max 50), description (textarea, optional, max 160), visibility radio (public/private). "Create Group" (primary) + "Discard Changes" (ghost). On submit calls `createGroup(name, description, visibility)`.
- `GroupCard`: row layout (display flex, alignItems center, height 56px, borderBottom '1px solid #f1f5f9'). Shows group name (16px #0f172a, flex 1) + description (14px #64748b, truncated, flex 1) + role badge (12px). Clicking the name/description area calls `loadMembers(groupId)` and sets local `selectedGroupId`.
  **Delete button (GRUP-02):** render only when `group.ownerId === currentUserId`. Button style: `{ fontSize: 12, color: '#dc2626', background: 'none', border: 'none', cursor: 'pointer', marginLeft: 'auto', padding: '4px 8px' }`. onClick: `e.stopPropagation()` to prevent loadMembers, then set local `confirmDeleteId = group.groupId`. When `confirmDeleteId === group.groupId`, render inline: "Delete group?" text + "Cancel" button (neutral compact, onClick clears confirmDeleteId) + "Delete" button (destructive compact, onClick calls `deleteGroup(groupId)` then clears confirmDeleteId).
- `MemberList`: shown below the selected GroupCard. Scrollable (max-height 200px, overflowY auto). Each member row: Avatar (32px) + displayName (16px) + role badge (12px #64748b). Role badge colors: owner → `{background: '#f0fdf4', color: '#16a34a'}`, admin → `{background: '#eff6ff', color: '#3b82f6'}`, member → `{background: '#f1f5f9', color: '#64748b'}`.
- `InviteForm`: input for userId + "Invite" button (primary compact). Shows only when role is owner/admin.

Main GroupPanel layout:
```
section card
  h2 "Groups" (sectionHeaderStyle)
  "Create Group" button (primary, full width, height 36, marginBottom 12)
    → toggles CreateGroupForm inline below
  GroupList (max-height 320px, overflowY auto):
    GroupCard × groups.length  OR  empty state (heading "No groups yet", body "Create a group or join a public one.")
  if selectedGroupId: MemberList + InviteForm
```

---

**RoomList.tsx — new file**

Props: `{ idToken: string | null; onMessage: OnMessageFn; onRoomSelect: (room: RoomItem) => void; activeRoomId?: string | null }`.

```tsx
type OnMessageFn = (handler: (msg: import('../types/gateway').GatewayMessage) => void) => () => void;

interface RoomListProps {
  idToken: string | null;
  onMessage: OnMessageFn;
  onRoomSelect: (room: RoomItem) => void;
  activeRoomId?: string | null;
}

export function RoomList({ idToken, onMessage, onRoomSelect, activeRoomId }: RoomListProps)
```

Inside: call `useRooms({ idToken, onMessage })`. The `onMessage` prop is forwarded directly to the hook so that `social:member_joined` and `social:member_left` events are handled inside `useRooms` (RTIM-04) without extra wiring in the component.

Sub-components (all unexported internals):
- `CreateRoomForm`: inline form with name input (required, max 50) + "Create Room" button. Appears when "Create Room" is clicked.
- `DMRoomButton`: small form with peerId input (Cognito userId) + "Open DM" button (neutral).
- `RoomRow` (internal, avoids name clash with RoomItem type): list row (height 56px, borderBottom). Room name (16px) + type badge (12px: standalone → #646cff, group → #3b82f6, dm → #16a34a, on respective light backgrounds) + member-count placeholder. Active room: `{ background: '#f1f5f9', borderLeft: '3px solid #646cff', paddingLeft: 13 }`. Click → call `onRoomSelect(room)`.

Main RoomList layout:
```
section card
  h2 "Rooms" (sectionHeaderStyle)
  "Create Room" button (primary, full width, height 36, marginBottom 8)
    → toggles CreateRoomForm inline below
  DMRoomButton inline (neutral, full width, height 36, marginBottom 12)
  Room items list (max-height 320px, overflowY auto):
    RoomRow × rooms.length  OR  empty state (heading "No rooms yet", body "Create a room or ask to be added.")
```
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/websocker_gateway/frontend && npx tsc --noEmit 2>&1 | grep -E "SocialPanel|GroupPanel|RoomList" | head -20; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "useSocialProfile" frontend/src/components/SocialPanel.tsx` returns a match
    - `grep -n "useFriends" frontend/src/components/SocialPanel.tsx` returns a match
    - `grep -n "MockDataBanner\|MOCK_USERS\|CURRENT_USER" frontend/src/components/SocialPanel.tsx` returns no matches
    - `grep -n "export function GroupPanel" frontend/src/components/GroupPanel.tsx` returns a match
    - `grep -n "useGroups" frontend/src/components/GroupPanel.tsx` returns a match
    - `grep -n "deleteGroup" frontend/src/components/GroupPanel.tsx` returns a match (GRUP-02 — delete button calls deleteGroup)
    - `grep -n "dc2626" frontend/src/components/GroupPanel.tsx` returns a match (destructive delete button color)
    - `grep -n "ownerId.*currentUserId\|currentUserId.*ownerId" frontend/src/components/GroupPanel.tsx` returns a match (owner-only visibility guard)
    - `grep -n "export function RoomList" frontend/src/components/RoomList.tsx` returns a match
    - `grep -n "useRooms" frontend/src/components/RoomList.tsx` returns a match
    - `grep -n "onMessage" frontend/src/components/RoomList.tsx` returns at least 2 matches (prop declaration + forwarded to useRooms)
    - `grep -n "class=\|className=\|import.*\.css" frontend/src/components/GroupPanel.tsx` returns no matches (inline styles only)
    - `npx tsc --noEmit` in frontend/ produces zero errors for these three files
  </acceptance_criteria>
  <done>SocialPanel uses live hooks with no mock data. GroupPanel shows Delete button for owners and calls deleteGroup (GRUP-02). RoomList forwards onMessage to useRooms (RTIM-04). All three compile cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create PostFeed and CommentThread components</name>
  <files>
    frontend/src/components/PostFeed.tsx
    frontend/src/components/CommentThread.tsx
  </files>
  <read_first>
    - .planning/phases/32-frontend-social-layer/32-UI-SPEC.md (PostFeed and CommentThread layout contracts, real-time update rules, copywriting contract)
    - frontend/src/components/ReactionButtons.tsx (EMOJIS array — must reuse same 12 values, same display style)
    - frontend/src/hooks/usePosts.ts (just created — return shape)
    - frontend/src/hooks/useComments.ts (just created — return shape)
    - frontend/src/hooks/useLikes.ts (just created — return shape)
  </read_first>
  <action>
**PostFeed.tsx — new file**

Props:
```tsx
interface PostFeedProps {
  idToken: string | null;
  roomId: string | null;
  onMessage: OnMessageFn;
}
export function PostFeed({ idToken, roomId, onMessage }: PostFeedProps)
```

Inside: call `usePosts({ idToken, roomId, onMessage })`.

Sub-components (all unexported internals):

- `CreatePostForm`: textarea (rows=3, placeholder="What's on your mind?", maxLength=10000, resize vertical, style matches UI-SPEC input defaults). Row below: "Post" button (primary, height 36, disabled when content empty or loading). Loading state: button text "Posting…", disabled. On submit: call `createPost(content)`, clear textarea on success.

- `PostCard`: renders one PostItem.
  - Header row: Avatar(32px) + displayName (16px #0f172a weight 600) + timestamp (14px #9ca3af, format as relative time e.g. "2m ago" using `Date.now() - new Date(createdAt).getTime()` threshold: <60s → "just now", <3600s → "Nm ago", else ISO date slice 0-10).
  - Body: content (16px #374151, marginTop 8, marginBottom 8).
  - Fade-in: if `post._fadeIn`, wrap in a div with `opacity` transitioning from 0 to 1. Use `useState(post._fadeIn ? 0 : 1)` + `useEffect(() => { if (post._fadeIn) setTimeout(() => setOpacity(1), 10); }, [])`, style `{ opacity, transition: 'opacity 200ms ease' }`.
  - PostActions row (display flex, gap 16, fontSize 14, marginTop 4):
    - LikeButton (internal): calls `useLikes({ idToken, roomId, postId: post.postId, onMessage })`. Renders `❤` glyph (style: fontSize 16, color: isLiked ? '#646cff' : '#9ca3af', cursor pointer) + count (color: isLiked ? '#646cff' : '#9ca3af'). onClick calls `toggle()`. aria-label `Like post`.
    - EmojiReactionBar (internal): same EMOJIS array as ReactionButtons.tsx — `['❤️','😂','👍','👎','😮','😢','😡','🎉','🔥','⚡','💯','🚀']`. Renders 12 small buttons (fontSize 16, background none, border none, padding '2px 4px', cursor pointer). onClick calls `reactWithEmoji(emoji)` from useLikes.
    - "Comment" text button (background none, border none, fontSize 14, color '#64748b', cursor pointer). onClick toggles `showComments` local state.
  - Own post (post.authorId matches decoded idToken sub): show "Delete" ghost button (fontSize 14, color '#9ca3af', background none, border none, cursor pointer) + inline confirm "Delete this post?" with "Cancel" (neutral) and "Delete" (destructive) buttons.
  - Edit: own post shows "Edit" ghost button. Clicking reveals inline textarea pre-filled with content + "Save" (primary) and "Discard Changes" buttons. On save: call `editPost(postId, content)`.
  - Expanded comments: if showComments, render `<CommentThread idToken={idToken} roomId={roomId} postId={post.postId} onMessage={onMessage} />` below PostCard.

PostFeed layout:
```
section card
  CreatePostForm
  divider (borderTop '1px solid #e2e8f0', margin '16px 0')
  hasMore: "Load more" button (neutral, full width) at top
  PostCard list (most recent first — posts already newest-first from hook)
    OR empty state: heading "No posts yet", body "Be the first to post in this room."
```

---

**CommentThread.tsx — new file**

Props:
```tsx
interface CommentThreadProps {
  idToken: string | null;
  roomId: string | null;
  postId: string | null;
  onMessage: OnMessageFn;
}
export function CommentThread({ idToken, roomId, postId, onMessage }: CommentThreadProps)
```

Inside: call `useComments({ idToken, roomId, postId, onMessage })`.

Sub-components (all unexported internals):

- `CommentItem`: renders one CommentItem.
  - Row: Avatar(32px compact) + div(flex 1): displayName (14px weight 600 #0f172a) + content (14px #374151) + timestamp (12px #9ca3af).
  - Indentation: if `comment.parentCommentId`, wrap in a div with `paddingLeft: 32`.
  - Fade-in: same pattern as PostCard (_fadeIn flag).
  - Own comment: "Delete" ghost button (14px #9ca3af, no border). Inline confirm "Delete this comment?" with "Cancel" and "Delete" (destructive).
  - "Reply" text button (14px #64748b, no border, no background) toggles inline ReplyForm.

- `ReplyForm`: single-line input (placeholder "Write a reply…", fontSize 14, style from UI-SPEC input defaults) + "Comment" button (primary compact height 28). On submit: call `createComment(content, parentCommentId)`.

- `ReplyForm` for top-level new comment: same component but without parentCommentId.

CommentThread layout:
```
container (paddingTop 8, paddingLeft 8, borderTop '1px solid #f1f5f9')
  if loading: "Loading comments…" (14px #9ca3af)
  CommentItem × comments.length (top-level first, replies indented 32px)
    OR empty state: "No comments yet" (14px #9ca3af, textAlign center, padding '16px 0')
  ReplyForm (top-level new comment at bottom)
```

Note: CommentThread is always rendered inline below PostCard — no card wrapper of its own.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/websocker_gateway/frontend && npx tsc --noEmit 2>&1 | grep -E "PostFeed|CommentThread" | head -20; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function PostFeed" frontend/src/components/PostFeed.tsx` returns a match
    - `grep -n "export function CommentThread" frontend/src/components/CommentThread.tsx` returns a match
    - `grep -n "usePosts" frontend/src/components/PostFeed.tsx` returns a match
    - `grep -n "useLikes" frontend/src/components/PostFeed.tsx` returns a match
    - `grep -n "CommentThread" frontend/src/components/PostFeed.tsx` returns a match (inline expand)
    - `grep -n "useComments" frontend/src/components/CommentThread.tsx` returns a match
    - `grep -n "social:post\|_fadeIn" frontend/src/components/PostFeed.tsx` returns a match (fade-in animation)
    - `grep -n "paddingLeft.*32\|paddingLeft: 32" frontend/src/components/CommentThread.tsx` returns a match (reply indentation)
    - `grep -n "class=\|className=\|import.*\.css" frontend/src/components/PostFeed.tsx` returns no matches
    - `npx tsc --noEmit` in frontend/ produces zero errors for these two files
  </acceptance_criteria>
  <done>PostFeed and CommentThread are created, compiled, use their respective hooks, implement real-time fade-in, and follow the UI-SPEC layout and copywriting contracts.</done>
</task>

</tasks>

<verification>
Run from the frontend/ directory:

```bash
cd /Users/connorhoehn/Projects/websocker_gateway/frontend && npx tsc --noEmit
```

Expected: zero TypeScript errors across all 5 component files.

```bash
grep -n "useSocialProfile\|useFriends" frontend/src/components/SocialPanel.tsx
grep -n "MockDataBanner\|MOCK_USERS" frontend/src/components/SocialPanel.tsx
```

Expected: first command has 2+ matches; second command has 0 matches.

```bash
for f in GroupPanel RoomList PostFeed CommentThread; do
  echo "=== $f ==="; grep -n "export function $f" frontend/src/components/$f.tsx; done
```

Expected: one export line per file.

```bash
grep -n "deleteGroup" frontend/src/components/GroupPanel.tsx
grep -n "onMessage" frontend/src/components/RoomList.tsx
```

Expected: at least one match each (GRUP-02 delete button, RTIM-04 onMessage forwarded to useRooms).

```bash
grep -rn "class=\|className=" frontend/src/components/GroupPanel.tsx frontend/src/components/RoomList.tsx frontend/src/components/PostFeed.tsx frontend/src/components/CommentThread.tsx
```

Expected: zero matches (inline styles only).
</verification>

<success_criteria>
- SocialPanel reads from live hooks, no mock data, no MockDataBanner
- GroupPanel, RoomList, PostFeed, CommentThread are new files with correct exported component names
- All 5 components compile with zero TypeScript errors
- GroupPanel's GroupCard shows Delete button (color #dc2626) visible only to group owner; on confirm calls deleteGroup (GRUP-02)
- RoomList accepts onMessage prop and forwards it to useRooms so social:member_joined / social:member_left update state (RTIM-04)
- PostFeed contains a CommentThread inline expansion and LikeButton + EmojiReactionBar
- CommentThread indents replies 32px and shows ReplyForm
- Real-time fade-in (200ms opacity transition) implemented in PostFeed and CommentThread for _fadeIn items
- Zero inline className= or external CSS imports
</success_criteria>

<output>
After completion, create `.planning/phases/32-frontend-social-layer/32-02-SUMMARY.md`
</output>
