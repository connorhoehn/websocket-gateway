---
phase: 19-per-service-dev-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/TabbedEventLog.tsx
autonomous: true
requirements:
  - DEV-01
  - DEV-02
  - DEV-03

must_haves:
  truths:
    - "Five distinct tabs exist: Chat, Presence, Cursors, Reactions, System"
    - "Each tab shows only events for its service (no cross-service noise)"
    - "Tabs are selectable independently"
    - "Events display with correct timestamps"
  artifacts:
    - path: "frontend/src/components/TabbedEventLog.tsx"
      provides: "TabbedEventLog component with tab filtering logic"
      exports: ["TabbedEventLog"]
  key_links:
    - from: "TabbedEventLog"
      to: "LogEntry"
      via: "Props entries array"
    - from: "Tab filter logic"
      to: "GatewayMessage.type"
      via: "Service-type classification"

---

<objective>
Create a tabbed EventLog component that separates real-time WebSocket events by service type (Chat, Presence, Cursors, Reactions, System), allowing developers to inspect each service's traffic independently without noise from other services.

Purpose: Phase 18 and earlier phases generated all events in a single flat list; developers need per-service visibility to debug individual features efficiently.

Output: TabbedEventLog.tsx component with 5 independent tabs, each rendering only its service's events with timestamps intact.
</objective>

<execution_context>
@/Users/connorhoehn/Projects/websocker_gateway/frontend/src/components/EventLog.tsx (current single-list implementation)
@/Users/connorhoehn/Projects/websocker_gateway/frontend/src/types/gateway.ts (GatewayMessage structure)
@/Users/connorhoehn/Projects/websocker_gateway/.planning/phases/18-typing-indicators-and-presence-polish/18-01-SUMMARY.md (latest pattern reference)
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Recent decision (Phase 16-17):** All UI uses inline JSX styles, no external CSS. @keyframes and styling live in JSX `<style>` tags or component inline styles. This pattern holds for TabbedEventLog.

**Service type mapping** (from gateway.ts and App.tsx log analysis):
- Chat: `type === 'chat'`
- Presence: `type === 'presence'`
- Cursors: `type === 'cursor'` (action may be 'moved', 'mode', etc.)
- Reactions: `type === 'reaction'`
- System: `type === 'error'` OR `type === 'session'` OR other gateway-level messages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TabbedEventLog component with tab state and filtering logic</name>
  <files>frontend/src/components/TabbedEventLog.tsx</files>
  <action>
Create a new component TabbedEventLog.tsx that:

1. **Props interface**: Accept `entries: LogEntry[]` (same as existing EventLog)

2. **Tab state**: Use useState to track active tab. Tabs are: 'Chat' | 'Presence' | 'Cursors' | 'Reactions' | 'System'. Default to 'Chat'.

3. **Tab filter logic**: Create a helper function `getTabEntries(entries: LogEntry[], tab: string): LogEntry[]` that:
   - Chat tab: entries where `message.type === 'chat'`
   - Presence tab: entries where `message.type === 'presence'`
   - Cursors tab: entries where `message.type === 'cursor'`
   - Reactions tab: entries where `message.type === 'reaction'`
   - System tab: entries where `message.type === 'error'` OR `message.type === 'session'` OR any other non-feature message (fallback: types not in the above four)

4. **Tab UI**: Render five tabs as horizontal buttons in a row at the top. Use inline styles:
   - Active tab: `background: '#3b82f6'`, `color: '#ffffff'`, `fontWeight: 600`
   - Inactive tab: `background: '#e5e7eb'`, `color: '#374151'`, `fontWeight: 400`
   - Each tab button: `border: 'none'`, `borderRadius: 4`, `padding: '0.5rem 1rem'`, `cursor: 'pointer'`, `fontSize: '0.875rem'`
   - Tab row container: `display: 'flex'`, `gap: '0.5rem'`, `marginBottom: '1rem'`, `borderBottom: '1px solid #e5e7eb'`, `paddingBottom: '1rem'`

5. **Entry display**: Reuse the existing single-entry rendering logic from EventLog.tsx — render each entry with direction badge, timestamp, and JSON payload. Keep the scrollable container (300px max-height) and auto-scroll behavior.

6. **Entry count badge**: Show count on each tab: "Chat (5)", "System (12)", etc. Update dynamically.

7. **Empty state**: If a tab has 0 entries, show "No events yet." in the same gray color as existing EventLog.

Use inline JSX styles throughout — no external CSS, consistent with phase decisions. Keep monospace font for log entries.

Reference existing EventLog.tsx styling for the entry card, direction badges, and timestamp formatting — the goal is visual consistency, just filtered by tab.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - TabbedEventLog.tsx exists and compiles with zero TypeScript errors
    - All 5 tabs render with correct labels and entry counts
    - Each tab correctly filters entries by service type
    - Entry styling matches existing EventLog.tsx (monospace, direction badges, timestamps, JSON display)
    - Tabs are selectable and state updates on click
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify TabbedEventLog filtering accuracy with test entries</name>
  <files>frontend/src/components/TabbedEventLog.tsx</files>
  <action>
Verify the component's filtering logic works correctly by inspecting the code:

1. Check the `getTabEntries` function logic by reading TabbedEventLog.tsx and confirming:
   - Chat filter: `message.type === 'chat'` captures outbound 'send_message' and inbound 'chat' messages
   - Presence filter: `message.type === 'presence'` captures presence updates
   - Cursors filter: `message.type === 'cursor'` captures cursor moves and mode changes
   - Reactions filter: `message.type === 'reaction'` captures reaction broadcasts
   - System filter: `message.type === 'error'` OR `message.type === 'session'` captures errors and session lifecycle

2. Verify filter chaining is correct:
   - No entry should appear in multiple tabs (filters are exclusive by service type)
   - If an entry doesn't match Chat/Presence/Cursors/Reactions, it routes to System tab

3. Verify the active tab state updates correctly when a tab button is clicked (useState logic is sound)

4. Verify TypeScript compilation passes: Run `npx tsc --noEmit` again to confirm zero errors after implementation.
  </action>
  <verify>
    <automated>grep -A 15 "getTabEntries\|function.*filter" frontend/src/components/TabbedEventLog.tsx | head -40</automated>
  </verify>
  <done>
    - Filter logic reviewed: each service type has a dedicated condition
    - No overlapping filters (no entry counted in multiple tabs)
    - TypeScript compilation: zero errors
    - Tab state mechanism (useState + onClick handlers) confirmed sound
  </done>
</task>

</tasks>

<verification>
Post-execution verification in Phase 19-02 when TabbedEventLog is wired into AppLayout:
- [ ] Five tabs render in the Dev Tools section with correct labels and counts
- [ ] Clicking each tab shows only events for that service
- [ ] Each tab's entry list includes all relevant messages with timestamps intact
- [ ] Disconnect/Reconnect controls remain visible in the sidebar (Phase 19-02 task)
- [ ] No cross-service noise: Chat tab shows 0 presence/cursor/reaction entries
</verification>

<success_criteria>
- TabbedEventLog.tsx component created and compiles with zero TypeScript errors
- All 5 tabs functional: Chat, Presence, Cursors, Reactions, System
- Entry filtering logic verified: each tab shows only its service's events
- Styling consistent with existing EventLog and phase design conventions (inline styles, monospace, gray/blue color palette)
- Component is a pure presentational component (no hook calls, all data via props) ready for integration
</success_criteria>

<output>
After completion, create `.planning/phases/19-per-service-dev-tools/19-01-SUMMARY.md`
</output>
