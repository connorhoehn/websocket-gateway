---
phase: 19-per-service-dev-tools
plan: 02
type: execute
wave: 1
depends_on:
  - 19-01
files_modified:
  - frontend/src/components/AppLayout.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "EventLog in Dev Tools section is replaced with TabbedEventLog"
    - "Five service tabs are visible and functional in the Dev Tools section"
    - "Disconnect/Reconnect controls remain visible in the sidebar"
    - "Dev Tools section maintains its layout and styling"
  artifacts:
    - path: "frontend/src/components/AppLayout.tsx"
      provides: "TabbedEventLog integration in Dev Tools section"
      modified: "EventLog import → TabbedEventLog, single <EventLog /> → <TabbedEventLog />"
  key_links:
    - from: "AppLayout"
      to: "TabbedEventLog"
      via: "logEntries prop passed to component"
    - from: "Dev Tools section"
      to: "DisconnectReconnect"
      via: "Sidebar placement unchanged"

---

<objective>
Wire TabbedEventLog component into AppLayout's Dev Tools section, replacing the existing single-list EventLog while ensuring disconnect/reconnect controls remain visible and accessible in the sidebar.

Purpose: Integrate the new tabbed event filtering to allow developers to see per-service traffic in the dev tools, without moving or hiding the connection control buttons.

Output: AppLayout updated to render TabbedEventLog in Dev Tools, with all styling and layout intact.
</objective>

<execution_context>
@/Users/connorhoehn/Projects/websocker_gateway/frontend/src/components/AppLayout.tsx (current Dev Tools section)
@/Users/connorhoehn/Projects/websocker_gateway/frontend/src/components/TabbedEventLog.tsx (newly created in Plan 19-01)
@/Users/connorhoehn/Projects/websocker_gateway/.planning/phases/17-ui-layout-and-polish/17-02-SUMMARY.md (AppLayout wiring pattern)
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**AppLayout context:**
- Lines 295-311: Dev Tools section currently renders ErrorDisplay, ErrorPanel, EventLog, and clientId/sessionToken footer
- Lines 213-232: Sidebar contains PresencePanel and DisconnectReconnect (both on left side)
- DisconnectReconnect must remain in sidebar for this phase to meet requirement DEV-03 ("controls remain accessible in the dev tools section")

**Integration pattern** (from Phase 17-02):
- Import replacement component at top
- Replace component usage in JSX
- Maintain prop passing (logEntries already exists)
- No layout changes needed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace EventLog with TabbedEventLog in AppLayout</name>
  <files>frontend/src/components/AppLayout.tsx</files>
  <action>
Update AppLayout.tsx:

1. **Update import** (line 29):
   - Current: `import { EventLog } from './EventLog';`
   - New: `import { TabbedEventLog } from './TabbedEventLog';`

2. **Update Dev Tools JSX** (lines 295-311):
   - Current: `<EventLog entries={logEntries} />`
   - New: `<TabbedEventLog entries={logEntries} />`
   - Keep all other Dev Tools children unchanged: ErrorDisplay, ErrorPanel, clientId/sessionToken footer
   - Keep the `<p style={sectionHeaderStyle}>Dev Tools</p>` header

3. **No other changes needed**:
   - Sidebar with DisconnectReconnect and PresencePanel remains untouched
   - All other section components (Chat, Cursors, Reactions, Shared Document) unchanged
   - Layout structure (2-column with header, sidebar, main) unchanged

4. **Verify structure**:
   - Dev Tools section maintains same card styling (sectionCardStyle)
   - TabbedEventLog receives same logEntries prop as EventLog did
   - Line count may increase slightly (tabs UI), but section remains in viewport
</action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "error\|warning" | head -10</automated>
  </verify>
  <done>
    - AppLayout.tsx imports TabbedEventLog (not EventLog)
    - Dev Tools section renders TabbedEventLog instead of EventLog
    - logEntries prop correctly passed to TabbedEventLog
    - TypeScript compilation: zero errors
    - Sidebar structure unchanged (DisconnectReconnect visible)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Dev Tools layout and visual consistency</name>
  <files>frontend/src/components/AppLayout.tsx</files>
  <action>
Verify the Dev Tools section integrates cleanly:

1. Check AppLayout.tsx lines 295-311 after modification:
   - Section header "Dev Tools" renders with sectionHeaderStyle (gray, uppercase, small font)
   - ErrorDisplay component renders first
   - ErrorPanel component renders second
   - TabbedEventLog component renders third with logEntries prop
   - Footer line with clientId/sessionToken renders last

2. Verify visual hierarchy is preserved:
   - Tab buttons from TabbedEventLog don't overflow or break layout
   - Section card maintains 1.25rem padding (sectionCardStyle)
   - Scrollable container in TabbedEventLog fits within the card

3. Check that sidebar (left side) remains unchanged:
   - PresencePanel at top of sidebar
   - DisconnectReconnect below PresencePanel (lines 227-231 untouched)
   - Sidebar width (240px) and styling unchanged

4. Verify no import errors:
   - Run `npx tsc --noEmit` to confirm TabbedEventLog import resolves
   - Confirm no unused imports (EventLog should be removed or commented)
  </action>
  <verify>
    <automated>grep -n "TabbedEventLog\|EventLog\|Dev Tools" frontend/src/components/AppLayout.tsx | head -20</automated>
  </verify>
  <done>
    - AppLayout Dev Tools section integrates TabbedEventLog cleanly
    - Section layout and styling unchanged
    - Sidebar structure preserved, DisconnectReconnect visible
    - TypeScript compilation passes (zero errors)
    - Visual hierarchy and spacing consistent with other sections
  </done>
</task>

</tasks>

<verification>
Phase-level verification (executed at end of Phase 19):
- [ ] Five tabs visible in Dev Tools: Chat, Presence, Cursors, Reactions, System
- [ ] Each tab filters events correctly (no cross-service noise)
- [ ] Disconnect and Reconnect buttons visible in sidebar (not moved)
- [ ] Dev Tools section layout remains compact and readable
- [ ] Timestamps and direction badges display correctly in each tab
- [ ] TypeScript compilation: zero errors
</verification>

<success_criteria>
- AppLayout.tsx compiles with zero TypeScript errors
- TabbedEventLog renders in place of EventLog in Dev Tools section
- Sidebar layout unchanged: DisconnectReconnect controls visible and accessible
- Dev Tools section maintains card styling and spacing
- Five tabs functional and filtering events per service type
- Visual consistency with existing UI maintained (inline styles, color palette, monospace fonts)
</success_criteria>

<output>
After completion, create `.planning/phases/19-per-service-dev-tools/19-02-SUMMARY.md`
</output>
