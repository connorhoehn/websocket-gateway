---
phase: 15-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/clients/test-client.html
  - test/clients/test-client-sdk.html
  - test/clients/test-client-multimode.html
  - test/clients/websocket-gateway-sdk.js
  - test/clients/websocket-gateway-sdk.css
  - .gitignore
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "No HTML test client files exist in test/clients/"
    - "No standalone SDK files (websocket-gateway-sdk.js / .css) exist anywhere in the repo"
    - "frontend/dist/ is gitignored and does not appear in git status output"
    - "git ls-files produces zero results for test/clients/*.html and websocket-gateway-sdk.*"
  artifacts:
    - path: "test/clients/"
      provides: "Directory should contain no .html, .js, or .css artifact files"
      contains: ""
    - path: ".gitignore"
      provides: "frontend/dist/ is explicitly covered (already present as dist/)"
  key_links:
    - from: "git index"
      to: "test/clients/ files"
      via: "git rm"
      pattern: "git rm.*test/clients"
    - from: ".gitignore"
      to: "frontend/dist/"
      via: "dist/ rule already present"
      pattern: "^dist/"
---

<objective>
Remove all HTML test clients, standalone SDK files, and stale build artifacts from the git-tracked repo.

Purpose: The repo should contain only source code going forward. HTML test clients served their purpose during early development and have been superseded by the React frontend. Leaving them tracked creates confusion about the intended entry point.
Output: Five tracked files deleted from git, frontend/dist/ confirmed gitignored, repo is artifact-free.
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tracked HTML and SDK files from git</name>
  <files>
    test/clients/test-client.html
    test/clients/test-client-sdk.html
    test/clients/test-client-multimode.html
    test/clients/websocket-gateway-sdk.js
    test/clients/websocket-gateway-sdk.css
  </files>
  <action>
    Delete the five tracked artifact files from the repository using `git rm`:

    ```
    git rm test/clients/test-client.html
    git rm test/clients/test-client-sdk.html
    git rm test/clients/test-client-multimode.html
    git rm test/clients/websocket-gateway-sdk.js
    git rm test/clients/websocket-gateway-sdk.css
    ```

    These files are HTML test clients and their associated SDK bundle that were used before the React frontend existed. They are tracked in git (confirmed via `git ls-files`) so `git rm` is the correct removal command — it both removes the file from disk and stages the removal.

    If test/clients/ becomes empty after removal, that is fine — leave the directory (git does not track empty directories anyway).

    Do NOT use plain `rm` — the goal is to stage the deletion so a follow-up commit records the removal in history.

    After running the commands, confirm with `git ls-files test/clients/` — expected output is empty.
  </action>
  <verify>
    <automated>git -C . ls-files test/clients/ | wc -l | grep -q '^0$' && echo "PASS: no tracked files in test/clients/" || echo "FAIL: files still tracked"</automated>
  </verify>
  <done>
    `git ls-files test/clients/` returns no output. The five files are staged for deletion. No HTML or SDK files remain tracked in the repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify frontend/dist/ is gitignored and confirm CLEAN-03</name>
  <files>.gitignore</files>
  <action>
    Perform two verifications and one targeted fix if needed:

    **Check 1 — frontend/dist/ gitignore status:**
    Run: `git check-ignore -v frontend/dist`

    The existing `.gitignore` has a `dist/` rule (confirmed). This covers `frontend/dist/` because git evaluates `.gitignore` patterns relative to the repo root. If `git check-ignore` confirms the path is ignored, no change to `.gitignore` is needed.

    If for any reason `frontend/dist/` is NOT ignored (unexpected), add an explicit entry to `.gitignore`:
    ```
    # Frontend build output
    frontend/dist/
    ```

    Also run: `git status --short | grep 'frontend/dist'` — expected: no output (directory either doesn't exist or is already ignored).

    **Check 2 — CLEAN-03 (test-client-sdk.html placeholder):**
    The roadmap references a `test-client-sdk.html` placeholder in the repo root. Verify it does not exist:
    `ls test-client-sdk.html 2>/dev/null || echo "not found"`

    The file was never tracked at the root (confirmed via `git ls-files`). The tracked version was at `test/clients/test-client-sdk.html` which Task 1 removes. No additional action needed for CLEAN-03 — it is satisfied by Task 1's deletion.

    Commit all staged changes (from Task 1 plus any .gitignore update) with message:
    `chore(15-cleanup): remove HTML test clients and SDK artifacts`
  </action>
  <verify>
    <automated>git check-ignore -v frontend/dist && git ls-files test/clients/ | wc -l | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - `git check-ignore frontend/dist` exits 0 (path is ignored)
    - `git status --short` shows no `frontend/dist` entry
    - `git ls-files | grep -E '\.html$|sdk\.(js|css)'` returns only `frontend/index.html` (the app entrypoint — this is correct and must NOT be deleted)
    - Deletion commit exists in git log
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks:

```bash
# 1. No test client HTML files tracked
git ls-files test/clients/

# 2. No SDK bundle files tracked
git ls-files | grep -E "websocket-gateway-sdk\.(js|css)"

# 3. frontend/dist/ is gitignored
git check-ignore -v frontend/dist

# 4. frontend/dist/ not in git status
git status --short | grep frontend/dist || echo "clean"

# 5. Commit recorded
git log --oneline -3 | head -3
```

Expected: commands 1 and 2 return empty, command 3 confirms ignore rule, command 4 returns "clean", command 5 shows the cleanup commit.
</verification>

<success_criteria>
- CLEAN-01: `git ls-files test/clients/` is empty; `git ls-files | grep websocket-gateway-sdk` is empty
- CLEAN-02: `git check-ignore frontend/dist` exits 0; `frontend/dist/` does not appear in `git status`
- CLEAN-03: `test-client-sdk.html` is absent from tracked files (removed as part of CLEAN-01)
- The repo's only tracked HTML file is `frontend/index.html` (the React app entrypoint)
</success_criteria>

<output>
After completion, create `.planning/phases/15-cleanup/15-01-SUMMARY.md` following the summary template.
</output>
