---
phase: 17-ui-layout-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/AppLayout.tsx
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04

must_haves:
  truths:
    - "AppLayout.tsx exists as an importable, self-contained component"
    - "The layout has a header row containing ConnectionStatus, ChannelSelector, user email, and sign-out button"
    - "The layout has a sidebar column containing PresencePanel"
    - "The layout has a main content area with clearly distinct named sections: Cursors, CRDT, Reactions, Chat"
    - "All props are passed in from outside — AppLayout has no internal hook calls or App.tsx-specific logic"
    - "AppLayout can be imported and used in any file without modification"
  artifacts:
    - path: "frontend/src/components/AppLayout.tsx"
      provides: "Structured 2-column app layout with header, sidebar, and main sections"
      exports: ["AppLayout", "AppLayoutProps"]
  key_links:
    - from: "frontend/src/components/AppLayout.tsx"
      to: "frontend/src/components/ConnectionStatus.tsx"
      via: "import + render in header"
      pattern: "import.*ConnectionStatus"
    - from: "frontend/src/components/AppLayout.tsx"
      to: "frontend/src/components/ChannelSelector.tsx"
      via: "import + render in header"
      pattern: "import.*ChannelSelector"
    - from: "frontend/src/components/AppLayout.tsx"
      to: "frontend/src/components/PresencePanel.tsx"
      via: "import + render in sidebar"
      pattern: "import.*PresencePanel"
---

<objective>
Create AppLayout.tsx — a structured, production-quality 2-column layout component that houses all collaborative feature sections. This component replaces the monolithic vertical stack currently in GatewayDemo.

Purpose: Extract layout from App.tsx so the layout itself is reusable. All data flows in through props, no hook calls inside AppLayout.

Output: frontend/src/components/AppLayout.tsx with AppLayout component and AppLayoutProps type
</objective>

<execution_context>
@/Users/connorhoehn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/connorhoehn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Relevant existing components (read these before implementing — use their prop interfaces exactly):
@frontend/src/components/ConnectionStatus.tsx
@frontend/src/components/ChannelSelector.tsx
@frontend/src/components/PresencePanel.tsx
@frontend/src/components/DisconnectReconnect.tsx
@frontend/src/components/ErrorDisplay.tsx
@frontend/src/components/ErrorPanel.tsx
@frontend/src/components/EventLog.tsx
@frontend/src/components/ReactionsOverlay.tsx
@frontend/src/components/ReactionButtons.tsx
@frontend/src/components/ChatPanel.tsx
@frontend/src/components/SharedTextEditor.tsx
@frontend/src/components/CursorModeSelector.tsx
@frontend/src/components/CursorCanvas.tsx
@frontend/src/components/TableCursorGrid.tsx
@frontend/src/components/TextCursorEditor.tsx
@frontend/src/components/CanvasCursorBoard.tsx

<interfaces>
<!-- Key contracts the executor must satisfy. All props flow in from outside. -->

From frontend/src/components/ConnectionStatus.tsx:
```typescript
import type { ConnectionState } from '../types/gateway';
interface Props { state: ConnectionState; }
export function ConnectionStatus({ state }: Props)
```

From frontend/src/components/ChannelSelector.tsx:
```typescript
interface Props { currentChannel: string; onSwitch: (channel: string) => void; }
export function ChannelSelector({ currentChannel, onSwitch }: Props)
```

From frontend/src/components/PresencePanel.tsx:
```typescript
import type { PresenceUser } from '../hooks/usePresence';
interface PresencePanelProps { users: PresenceUser[]; currentClientId: string | null; }
export function PresencePanel({ users, currentClientId }: PresencePanelProps)
```

From frontend/src/components/DisconnectReconnect.tsx:
```typescript
import type { ConnectionState } from '../types/gateway';
interface Props { connectionState: ConnectionState; onDisconnect: () => void; onReconnect: () => void; }
export function DisconnectReconnect({ connectionState, onDisconnect, onReconnect }: Props)
```

From frontend/src/components/ErrorPanel.tsx:
```typescript
// receives errors: GatewayError[]
export function ErrorPanel({ errors }: Props)
```

From frontend/src/components/EventLog.tsx:
```typescript
export interface LogEntry { id: string; direction: 'sent' | 'received'; message: ...; timestamp: string; }
interface Props { entries: LogEntry[]; }
export function EventLog({ entries }: Props)
```

From frontend/src/components/ReactionsOverlay.tsx:
```typescript
// receives reactions: ActiveReaction[]
export function ReactionsOverlay({ reactions }: Props)
```

From frontend/src/components/ReactionButtons.tsx:
```typescript
interface Props { onReact: (emoji: string) => void; disabled?: boolean; }
export function ReactionButtons({ onReact, disabled }: Props)
```

From frontend/src/components/ChatPanel.tsx:
```typescript
import type { ChatMessage } from '../hooks/useChat';
interface ChatPanelProps { messages: ChatMessage[]; onSend: (content: string) => void; disabled?: boolean; onTyping?: (isTyping: boolean) => void; }
export function ChatPanel({ messages, onSend, disabled, onTyping }: ChatPanelProps)
```

From frontend/src/components/SharedTextEditor.tsx:
```typescript
export interface SharedTextEditorProps { content: string; applyLocalEdit: (newText: string) => void; disabled?: boolean; }
export function SharedTextEditor({ content, applyLocalEdit, disabled }: SharedTextEditorProps)
```

From frontend/src/components/CursorModeSelector.tsx:
```typescript
// activeMode: CursorMode, onModeChange: (mode: CursorMode) => void
export function CursorModeSelector({ activeMode, onModeChange }: Props)
```

From frontend/src/hooks/useCursors.ts:
```typescript
export type CursorMode = 'freeform' | 'table' | 'text' | 'canvas';
export interface TextSelectionData { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppLayout.tsx with structured 2-column layout</name>
  <files>frontend/src/components/AppLayout.tsx</files>
  <action>
Create `frontend/src/components/AppLayout.tsx`. This is a pure presentational layout component — no hook calls, no internal state beyond what's needed for the cursor mode section.

**AppLayoutProps interface** — declare and export this type:
```typescript
export interface AppLayoutProps {
  // Connection/header
  connectionState: ConnectionState;
  currentChannel: string;
  onSwitchChannel: (channel: string) => void;
  onDisconnect: () => void;
  onReconnect: () => void;
  userEmail: string | null;
  onSignOut: () => void;

  // Presence sidebar
  presenceUsers: PresenceUser[];
  currentClientId: string | null;

  // Reactions overlay + buttons
  activeReactions: ActiveReaction[];
  onReact: (emoji: string) => void;

  // Chat
  chatMessages: ChatMessage[];
  onChatSend: (content: string) => void;
  onTyping?: (isTyping: boolean) => void;

  // Cursors
  cursors: CursorData[];
  activeMode: CursorMode;
  onModeChange: (mode: CursorMode) => void;
  onFreeformMove: (x: number, y: number) => void;
  onTableClick: (row: number, col: number) => void;
  onTextChange: (position: number, selectionData: TextSelectionData | null, hasSelection: boolean) => void;
  onCanvasMove: (x: number, y: number) => void;

  // CRDT
  crdtContent: string;
  applyLocalEdit: (newText: string) => void;

  // Dev tools
  logEntries: LogEntry[];
  errors: GatewayError[];
  lastError: GatewayError | null;
  clientId: string | null;
  sessionToken: string | null;
}
```

**Layout structure** (all inline styles, no external CSS, consistent with project convention):

1. **Outer wrapper**: `position: relative; min-height: 100vh; font-family: system-ui, -apple-system, sans-serif; background: #f8fafc;`

2. **ReactionsOverlay** — render at top of wrapper (fixed-position, sits above everything): `<ReactionsOverlay reactions={activeReactions} />`

3. **Header bar**: `display: flex; align-items: center; justify-content: space-between; padding: 0.75rem 1.5rem; background: #ffffff; border-bottom: 1px solid #e2e8f0; gap: 1rem;`
   - Left side: App title (`WebSocket Gateway`) in `font-size: 1rem; font-weight: 600; color: #0f172a;`
   - Center: `<ConnectionStatus state={connectionState} />` then `<ChannelSelector currentChannel={currentChannel} onSwitch={onSwitchChannel} />`
   - Right side: user email in `font-size: 0.875rem; color: #64748b;` then Sign Out button styled cleanly (not monospace, normal font, `background: none; border: 1px solid #e2e8f0; border-radius: 6px; padding: 0.25rem 0.75rem; cursor: pointer; font-size: 0.875rem; color: #374151;`)

4. **Body** (below header): `display: flex; min-height: calc(100vh - 53px);`

5. **Sidebar** (left column): `width: 240px; flex-shrink: 0; padding: 1rem; border-right: 1px solid #e2e8f0; background: #ffffff; display: flex; flex-direction: column; gap: 1rem;`
   - `<PresencePanel users={presenceUsers} currentClientId={currentClientId} />`
   - DisconnectReconnect controls in the sidebar below presence: `<DisconnectReconnect connectionState={connectionState} onDisconnect={onDisconnect} onReconnect={onReconnect} />`

6. **Main content** (right column): `flex: 1; padding: 1.5rem; display: flex; flex-direction: column; gap: 1.5rem; overflow-y: auto;`

   Each feature section uses a shared section card style: `background: #ffffff; border: 1px solid #e2e8f0; border-radius: 8px; padding: 1.25rem;`

   Each section has a section header: `font-size: 0.75rem; font-weight: 600; text-transform: uppercase; letter-spacing: 0.05em; color: #64748b; margin: 0 0 0.75rem 0;`

   Section order:
   a. **Chat section** — header "Chat", renders `<ChatPanel messages={chatMessages} onSend={onChatSend} disabled={connectionState !== 'connected'} onTyping={onTyping} />`
   b. **Presence section** — NOT in main (it's in sidebar). Skip.
   c. **Cursors section** — header "Cursors", renders `<CursorModeSelector activeMode={activeMode} onModeChange={onModeChange} />` then the active cursor panel (freeform/table/text/canvas conditional rendering, same logic as current App.tsx)
   d. **Reactions section** — header "Reactions", renders `<ReactionButtons onReact={onReact} disabled={connectionState !== 'connected'} />`
   e. **Shared Document (CRDT) section** — header "Shared Document", renders `<SharedTextEditor content={crdtContent} applyLocalEdit={applyLocalEdit} disabled={connectionState !== 'connected'} />`

7. **Dev Tools section** (inside main, at bottom, below the feature sections): section card with header "Dev Tools". Contains:
   - `<ErrorDisplay error={lastError} />`
   - `<ErrorPanel errors={errors} />`
   - `<EventLog entries={logEntries} />`
   - Debug info line: `clientId: {clientId ?? '—'} | sessionToken: {sessionToken ? sessionToken.slice(0, 8) + '…' : '—'}` in `font-size: 0.75rem; color: #94a3b8; font-family: monospace;`

**Import requirements**: Import all components used. Import types from their source files — `ConnectionState`, `GatewayError` from `../types/gateway`; `PresenceUser` from `../hooks/usePresence`; `ActiveReaction` from `../hooks/useReactions` (or wherever it's exported — check the file); `ChatMessage` from `../hooks/useChat`; `CursorMode`, `TextSelectionData`, `CursorData` from `../hooks/useCursors`; `LogEntry` from `./EventLog`.

Before writing the file, grep the source files for the actual export names of `ActiveReaction` and `CursorData` to ensure the import paths are correct.
  </action>
  <verify>
    <automated>cd /Users/connorhoehn/Projects/websocker_gateway/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>AppLayout.tsx exists, TypeScript compiles with no errors on AppLayout.tsx, all required sections (Chat, Cursors, Reactions, Shared Document, Dev Tools) are present, header contains ConnectionStatus + ChannelSelector + user controls, sidebar contains PresencePanel + DisconnectReconnect</done>
</task>

</tasks>

<verification>
Run `cd /Users/connorhoehn/Projects/websocker_gateway/frontend && npx tsc --noEmit` — zero TypeScript errors.
Grep: `grep -n "section\|header\|sidebar\|Chat\|Cursor\|Reaction\|CRDT\|DevTools" frontend/src/components/AppLayout.tsx` — confirms all layout regions present.
</verification>

<success_criteria>
- AppLayout.tsx exists and TypeScript compiles cleanly
- AppLayoutProps is exported and typed (no `any`)
- Header includes: ConnectionStatus, ChannelSelector, user email, Sign Out button
- Sidebar includes: PresencePanel, DisconnectReconnect
- Main area has distinct sections: Chat, Cursors, Reactions, Shared Document, Dev Tools
- No hook calls inside AppLayout — pure presentational
- All imports use relative paths from ../hooks/ and ../types/ as appropriate
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-layout-and-polish/17-01-SUMMARY.md`
</output>
