---
phase: 06-foundation
task: deployment-fixes
status: in_progress
last_updated: 2026-03-04T15:43:12.123Z
---

<current_state>
Running `make cdk-watch` to deploy ARM64 + deployment circuit breaker update to WebsockerGatewayStack.
Stack is currently UPDATE_IN_PROGRESS. The stack was CREATE_COMPLETE before this update started.
The CDK update will recreate the ECS service (previously manually deleted during debugging) with the correct ARM64 platform.
</current_state>

<completed_work>

Phase 06 (React frontend) was already fully complete before this session. This session was entirely deployment/infra fixes:

- `lib/cognito.ts` — new CDK construct: UserPool + UserPoolClient (wsgateway-server, no secret, USER_PASSWORD_AUTH)
- `lib/websocket-gateway-stack.ts` — wires Cognito in, passes pool ID/region to task def, adds CognitoUserPoolId + CognitoClientId CfnOutputs
- `lib/task-definition.ts` — accepts cognitoUserPoolId/cognitoRegion props; added ARM64 runtimePlatform
- `lib/dynamodb-table.ts` — switched to `Table.fromTableName()` (crdt-snapshots already exists in AWS, can't recreate)
- `lib/fargate-service.ts` — HTTPS listener conditional on ACM_CERTIFICATE_ARN (HTTP-only fallback for dev); added circuitBreaker + minHealthyPercent:100
- `tsconfig.json` — added `frontend` and `test` to exclude (was causing 77 build errors from React/DOM types)
- `Makefile` — fixed `cdk-deploy` npm arg passing (`--`); added `cdk-watch`, `deploy-image`, `deploy-image-tail`, `ecs-status`, `ecs-logs` targets
- `scripts/generate-env.sh` — reads CognitoUserPoolId/CognitoClientId from CF outputs when stack deployed
- `scripts/deploy-image.sh` — build linux/arm64, push ECR, force ECS deploy, watch for new task failures
- `scripts/watch-deploy.sh` — runs cdk deploy + monitors ECS stopped tasks in parallel, surfaces crash + logs immediately

AWS state at pause:
- Stack: UPDATE_IN_PROGRESS (CDK deploy running in terminal)
- New Cognito pool: `us-east-1_1cBzDswEa`, client: `4bcsu1t495schc9fi25ompnv9j`
- ECR image: arm64, tag `latest`, pushed 2026-03-03

</completed_work>

<remaining_work>

1. **Wait for `make cdk-watch` in terminal to complete**
2. **If UPDATE_COMPLETE** → `make gen-env` then `make dev-real` to verify local gateway with real Cognito
3. **If failed** → `make ecs-logs` to see crash; most likely causes: missing env var or startup crash
4. **Fix `watch-deploy.sh` false-positive alerts** — monitor surfaces old stopped tasks; needs to filter by tasks stopped AFTER deploy start time
5. **Commit all changes** once stack verified stable (nothing committed this session)

</remaining_work>

<decisions_made>

- `Table.fromTableName()` not `cdk import` — unblocks deploy, table schema already correct
- HTTPS conditional on ACM_CERTIFICATE_ARN — dev deploy works without a domain
- ARM64 on task def — image built on Apple Silicon, no rebuild needed
- `circuitBreaker: { rollback: true }` — ECS deploy failures in ~2 min not looping forever
- Cognito UserPool `removalPolicy: RETAIN` — users survive `cdk destroy`
- Old pool `us-east-1_CxlCbd1Zq` orphaned (had no app clients) — safe to delete from console

Chain of deploy failures fixed this session:
1. No Cognito app client → added to CDK
2. tsconfig included frontend → excluded
3. Makefile npm `--` bug → fixed
4. DynamoDB already existed → fromTableName
5. Placeholder cert ARN → conditional listener
6. ARM64 image on x86_64 task → ARM64 platform added
7. ECS looping with no circuit breaker → circuitBreaker added

</decisions_made>

<blockers>

- No blockers — deploy in progress in user's terminal

</blockers>

<context>
Session goal was to get `make dev-real` working (local gateway + real Cognito). This required fixing a chain of CDK deploy failures. All code fixes are in place and uncommitted. The running `make cdk-watch` in the terminal is the final step — once UPDATE_COMPLETE, the stack has everything needed to run `make gen-env` + `make dev-real`.
</context>

<next_action>
Check if `make cdk-watch` completed:
  aws cloudformation describe-stacks --stack-name WebsockerGatewayStack --region us-east-1 --query 'Stacks[0].StackStatus' --output text

If UPDATE_COMPLETE → run `make gen-env` then `make dev-real`
If still in progress → wait, monitor with `make ecs-status`
If failed → run `make ecs-logs` to see crash reason
</next_action>
