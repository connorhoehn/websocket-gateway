# Local development overrides for Colima + Tilt
gateway:
  image:
    pullPolicy: Never  # Use locally-built images
  env:
    SKIP_AUTH: "true"
    LOG_LEVEL: debug

socialApi:
  image:
    pullPolicy: Never
  env:
    VNL_API_URL: "https://lo0x6g47m6.execute-api.us-east-1.amazonaws.com/prod"
    VNL_COGNITO_USER_POOL_CLIENT_ID: "1ilql0o1ah4a091drorsbpcaq8"
    VNL_SERVICE_USERNAME: "connorhoehn3"
    VNL_SERVICE_PASSWORD: "Cbhoehn20!"
